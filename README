How to use:

Instantiate the Docker branch using this GitHub repository on Cloudlab.

------------------------------------------------------------------

To run Linpack, execute the following:

sudo docker run -it --rm --privileged linpack

Run the benchmark with manual input for parameters.
The test case parameters were: 

Number of equations to solve: 45000

Leading dimension of array: 45000

Number of trials to run: 10

Data alignment value (in Kbytes): 1


----------------------------------------------------------------

To run Stream, run the following:

sudo docker run --rm --privileged stream

-----------------------------------------------------------------
